{
  "name": "base-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "uniswap": "ts-node ./src/apps/uniswap.ts",
    "aave": "ts-node ./src/apps/aave.ts",
    "docker:build": "docker build -t bot .",
    "docker:run": "docker run -it -d --name=airdrop-bot bot",
    "docker:delete": "docker rm -f airdrop-bot && docker image rm -f bot",
    "docker:logs": "docker logs -f airdrop-bot",
    "docker:rebuild": "npm run docker:delete && npm run docker:build && npm run docker:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dotenvx/dotenvx": "^0.35.1",
    "@types/node": "^20.12.7",
    "cron": "^3.1.7",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "viem": "^2.9.22"
  },
  "devDependencies": {
    "tsconfig-paths": "^4.2.0"
  }
}
